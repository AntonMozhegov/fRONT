export const lab_5 = `
<h1>Лабораторная работа<p><strong>Цель:</strong> Освоить базовые навыки запуска PostgreSQL в контейнере Docker и настройку удалённого подключения к базе данных из IDE IntelliJ IDEA.</p> <h2>1.1: Проверка и установка Docker</h2> <ol> <li>Откройте терминал (Linux/macOS) или командную строку/PowerShell (Windows).</li> <li>Проверьте наличие установленного Docker: <pre><code>docker --version</code></pre> </li> <li>Если Docker не установлен: <ul> <li>Скачайте Docker Desktop с <a href="https://www.docker.com/products/docker-desktop ">официального сайта</a>.</li> <li>Установите, следуя инструкциям мастера установки.</li> <li>Для Linux может потребоваться дополнительная настройка: <pre><code>sudo systemctl start docker</code></pre> </li> </ul> </li> </ol> <h2>1.2: Проверка Docker Compose</h2> <ol> <li>Проверьте наличие Docker Compose: <pre><code>docker-compose --version</code></pre><h2>2.1: Создание рабочей директории</h2> <ol> <li>Создайте папку для проекта: <ul> <li>Linux/macOS: <pre><code>mkdir ~/postgres_docker && cd ~/postgres_docker</code></pre> </li> <li>Windows: <pre><code>mkdir C:\\postgres_docker && cd C:\\postgres_docker</code></pre> </li> </ul> </li> </ol> <h2>2.<h2>2.<h2>3.<h2>3.2: Работа с базой<h2>3.1: Настройкафайла docker-compose.yml</h2> <ol> <li>Создайте файл конфигурации: <pre><code>nano docker-compose.yml</code></pre> </li> <li>Вставьте следующую конфигурацию: <pre><code>version: '3.8' services: postgres: image: postgres:15 environment: POSTGRES_USER: admin POSTGRES_PASSWORD: admin POSTGRES_DB: test_db ports: - "5432:54<h2>2.3: Зап1: Настройка<h2>4: Завершение работы</h2> <ol> <li>Остановите контейнер: <pre><code>docker-compose down</code></pre> </li> <li>Для полной очистки (включая volume): <pre><code>docker-compose down -v</code></pre> </li> <li>Проверьте остановку: <pre><code>docker ps -a</code></pre> </li> </ol> <h2>Контрольные вопросы:</h2> <ul> <li>Какие преимущества даёт использование Docker при разработке?</li> <li>Как работает механизм volumes в Docker?</li> <li>Какие альтернативы IntelliJ IDEA существуют для подключения к PostgreSQL?</li> <li>Как проверить доступность БД после запуска контейнера?</li> </ul> <h2>Вывод:</h2> <p>В ходе выполнения лабораторной работы были освоены базовые навыки работы с Docker, создан контейнер PostgreSQL, выполнено его подключение из среды разработки IntelliJ IDEA. Полученные знания позволят использовать контейнеризацию в дальнейших проектах и тестировать приложения без необходимости локальной установки СУБД.</p>


`;